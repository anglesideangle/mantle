{ lib, ... }:
{
  # boot.uki.name = "amos";
  # boot.uki.tries = 2;

  # system.image.id = "amos";
  # system.nixos.distroId = "amos";
  # system.nixos.distroName = "AmOS";

  # networking.hostName = "mit-fsae";

}
